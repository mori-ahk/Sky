// ====== Class declarations ====== //
class POLYNOMIAL {
	public evaluate(float x, integer y, float c) : float;
	public float x;
};

// ====== Function Definitions ====== //
POLYNOMIAL::evaluate(float x) : float
local
    integer n;
    integer i;
  do
    return (0);
  end

printArray(integer arr[], integer size) : void
  local
    integer n;
    integer i;
  do
    n = size;
    i = 0;
    while (i<n)
      do
        write(arr[i]);
        i = i+1;
      end;
  end

main
  local
    linear f1;
  do
    f1 = f1.build(-2.0, 1.0, 0.0);
    counter = 1;
  end
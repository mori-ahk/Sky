main
local
    integer n;
    float i;
do

    //this line generate unmatched type error,
    //since each side of the compare expression has
    //different types
    while (n < i)
    do
    end;
end
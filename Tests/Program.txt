class LINEAR inherits POLYNOMIAL {
	private float a;
	private float b;
    private A z;

	public build(float A, float B) : LINEAR;
	public evaluate(float x) : float;
	public evaluate(integer y) : float;
};

class POLYNOMIAL {
    private float x;
};

LINEAR::build(float A, float B) : LINEAR
    do
    end

LINEAR::evaluate(float x) : float
    local
        integer a;
    do
    end

LINEAR::evaluate(integer y) : float
    do
    end

LINEAR::x() : void
    local
        integer a;
    do
    end

A::b() : void
do
end

/* sort the array */
bubbleSort(integer arr, integer size) : void
  local
    integer n;
    integer i;
    integer j;
    integer temp;
  do

  end

/* Print the array */
printArray(integer arr[], integer size) : void
  local
    integer a;
    integer i;
  do
   bubbleSort(a, size);
  end

printArray(integer arr[], integer size) : void
  local
    integer a;
    integer i;
  do
   bubbleSort(a, size);
  end
// main funtion to test above
main
  local
    integer arr[][7];
  do
    arr[0] = 64;
  end